{
  "private": true,
  "scripts": {
    "webpack": "webpack --hide-modules --progress",
    "dev": "webpack --hide-modules --progress --watch",
    "production": "export NODE_ENV=production && webpack --hide-modules --progress"
  },
  "devDependencies": {
    "laravel-mix": "^0.4.0"
  },
  "dependencies": {
    "axios": "^0.15.3",
    "vue": "^2.0.1",
    "vue-resource": "^1.0.3"
  }
}
